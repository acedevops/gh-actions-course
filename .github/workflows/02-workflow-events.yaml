name: 02-workflow-events
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '*/1 * * * *'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
          echo "Trigger event name is: ${{ github.event_name }}"
